
/**
 * The map() method is used for creating a new array from the existing one by applying
 * a function to each one of the elements of the first array.
 */

let nums = [3,8,12,16];
let multy = nums.map((num) => num * num);

console.log(multy); //output: [9,64,144,256]